# Як протестувати Backend?

Source: https://www.youtube.com/watch?v=itAosTFaG4c

початківці часто не розуміють як тестувати бекенд тому давайте коротко сьогодні розберемо цю [музика] тему отже по тестуванню бекенда в нас є мінімум дві теми це тестування API та тестування базданих почнемо з тестування API як протестувати типовий API запит який нам дали на роботі перший тест який ми обов'язково напишемо - це позитивний тест коли ми вводимо абсолютно все за документацією правильний API endpint потрібні параметри якщо потрібно то заголовки авторизацію і Очікуємо на успішний статус-код Тобто ми очікуємо що API запит поверне нам 200 Ок і правильний respon Body тобто тіло з очікуваним результатом другий варіант як можна протестувати API - це вже негативний тест ми можемо не передати необхідних параметрів Тобто ми передаємо API URL посилання ми можемо передати хедери всі інші дані які потрібно там тип запиту Але ми не передаємо параметри І що ми очікуємо ми очікуємо статус-код 400 Bad request або щось із 400ї групи статус-кодів третій тест можна написати тест з некоректними параметрами Наприклад якщо у нас в параметрах очікувалося строку strрінг А ми передаємо число inнt або навпаки очікувалось число А ми передаємо текст так само ми о очікуємо побачити статус 400 Bad request або щось подібне наступний - це тест на некоректні параметри ми можемо спробувати передати в якості параметрів якісь некоректні дані наприклад не існуючий ID якщо ви згадаєте приклад з пецстором де ми створювали тваринок і діставали про них інформацію то ми могли би створити тваринку з id1 23 і перевірити що не існує тваринки з id1 2 4 Якщо її не існує то відправити Get на саме це не існую ID тобто на ID тваринки якої немає в базі і перевірити що ми отримали статус-код 404 not found тобто таких даних з id1 24 в базі даних в нас немає наступний тест - це тест на авторизацію якщо наш apiзапит вимагає дані з авторизації наприклад username та пароль ми можемо спробувати відправити API запит без них тобто не передавши їх що ми очікуємо ми очікуємо помилку 401 авй і ми мусимо перевірити що якщо ми не ввели логін та пароль нам і не покаже потрібну інформацію або запит просто не виконається Крім того ми можемо провести тест на швидкість Відправити один і той самий apiзапит декілька разів і перевірити час відповіді і порівняти ці часи відповіді і переконатися в тому що всі відповіді прийшли в адекватних часових рамках тобто точно менше однієї секунди а то і ще менше Ось так Крім того ми можемо провести тест на на обсяг даних ми передаємо API запит в тіло якого ми засовуємо дуже велику кількість об'єктів Наприклад якщо ми хочемо гетнути то ми передаємо ледь не всі не всіх тваринок або якщо ми хочемо створити Ми обираємо наприклад Create with array створюємо масив даних і передаємо туди тисячі тваринок і ми перевіряємо як поведе себе apiзапит і чи дійсно він створить цю 1ся тваринок і останнє можна провести тест на безпеку для цього в API запит ми можемо в якості параметрів вставляти SQL xss ін'єкції і перевіряти чи наш API вразливий до них наприклад вставити якусь команду SQL в якості параметра і перевірити що ця команда в нас не виконується ось такий набросочок з тестів API як щодо баз даних Спитаєте ви А з базами даних все ще простіше Справа в тому що ми не тестуємо бази даних як такі якщо ми тільки не працюємо на проекті який розробляє база даних в принципі як тестувальники ми використовуємо бази даних але не тестуємо їх роботу що це означає Ну як тестувати це дуже просто ми створили користувача ми Заходимо в базу даних і пишемо Select все from користувачі де ім'я такето і ми просто перевіряємо що в базі даних цей користувач був створений і все тобто якщо ми створили щось на юзер-інтерфейсі потрібно перевірити що воно є в базі даних якщо ми видалили щось з бази даних ми перевіряємо що воно видалене з юзер-інтерфейсу і це власне всі тести на базу даних тому що там особливо нема що тестувати ми просто перевіряємо що дані на юзер-інтерфейсі повністю співпадають з даними в базі даних і це все я дякую вам за перегляд такого короткого але дуже корисного уроку будь ласка залишайте коментарі вподобайки розкажіть про цей канал своїм друзям і обов'язково Підписуйтесь це допомагає мені знімати Нове відео і надає мені мотивацію для роботи Дякую вам за перегляд і до нових зустрічей
